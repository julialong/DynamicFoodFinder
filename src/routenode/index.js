 
 const express = require('express');
 const bodyParser = require('body-parser');
 const calculator = require('RouteCalculator');
 const app = express();
 app.use(bodyParser.json());

app.post('/index', function(request, result) {
        
    })